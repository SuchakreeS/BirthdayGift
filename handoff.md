# handoff.md — project timeline

## Entry 1 — initial state
**Done:**
- Defined full project flow and content in `plan.md`
- Built and delivered a static HTML draft of the PIN screen (elegant/minimal theme, wine + gold + ivory palette, Cormorant Garamond + Jost fonts, line-art seal signature element). PIN `0803` with 3 Thai hints revealed progressively on wrong attempts.

**Current state relative to build order:**
- Step 1 (PIN page) — drafted in plain HTML/CSS/JS, not yet ported to Next.js/React.
- Steps 2–5 — not started.

**Unfinished / partial:**
- User asked for the PIN page to be rebuilt in React/Next.js — not yet done, this is the next task.

**Open questions for user (not blocking):**
- Hosting/deployment target for the finished site — unknown, ask before deploy step.
- Quiz questions, photos, song mp3, and reward details are all still pending from the user (see `plan.md` pending content list).

**Next step:**
Port the existing PIN page design/behavior into a Next.js project structure (React component), preserving the same visuals, PIN logic, and hint behavior. Confirm with the user where they want the Next.js project scaffolded before creating files, since no project directory exists yet.

## Entry 2 — 2026-08-13 — PIN page ported to Next.js

**Done:**
- Asked user where to scaffold (this folder) and confirmed the original static HTML draft is gone — rebuilt the PIN screen from the design tokens recorded in `plan.md` rather than a source file.
- Scaffolded a Next.js 16 (App Router, TypeScript, Tailwind v4) project directly in `Birthday4U/` via `create-next-app`. Had to bootstrap into a temp lowercase-named folder first (npm rejects capital letters in package names) and move the files in, since `Birthday4U` itself has a capital letter.
- **Note:** the scaffold's own `app`/root `CLAUDE.md` file (a 1-line stub pointing at `AGENTS.md`) overwrote this project's real `CLAUDE.md` instructions file during the move. Caught it immediately and restored the original content from conversation context — no data was actually lost, but flagging this in case anyone notices `CLAUDE.md`'s mtime looks recent. Left the scaffold's `AGENTS.md` in place (it's regenerated by `next dev` itself and doesn't conflict).
- Fixed `package.json` name from the temp `birthday4u-tmp` to `birthday4u`.
- Set up design tokens as CSS variables in `app/globals.css` (ivory/wine/wine-deep/gold/blush/ink/ink-soft) mapped into Tailwind's `@theme`, plus keyframes for rise-in, shake, and soft-reveal per `plan.md`'s motion spec.
- Loaded Cormorant Garamond (display) + Jost (body) via `next/font/google` in `app/layout.tsx`.
- Built `app/components/Seal.tsx` — inline SVG line-art seal with an infinity-knot motif (no source asset existed, drawn from the plan's description).
- Built `app/components/PinScreen.tsx` — 4 separate digit inputs with auto-advance/backspace navigation, PIN `0803` check, shake animation + one Thai hint revealed per wrong attempt (capped at 3), gentle rise-in on load.
- Wired it into `app/page.tsx`: shows `PinScreen` until unlocked, then a placeholder "Unlocked ✦ / Quiz step coming next" screen (quiz itself isn't built yet — out of scope for this step).
- Verified: `tsc --noEmit` clean, `npm run dev` starts without errors, homepage returns 200 and compiles with no console/log errors. Dev server stopped after verification.

**Current state relative to build order:**
- Step 1 (PIN page) — **done**, ported to Next.js/React with working PIN + hint logic.
- Steps 2–5 — not started. `page.tsx` currently just shows a placeholder after unlock.

**Unfinished / partial:**
- No quiz page yet — unlock currently dead-ends at a placeholder screen.
- Reward reveal, photo album, and music are all untouched.

**Open questions for user (not blocking):**
- Hosting/deployment target — still unknown.
- Quiz questions, photos, song mp3, reward details — still pending from user per `plan.md`.
- Page `<title>`/metadata was set to a neutral "Birthday4U" — not a flow-visible content decision, but flag if you want something else.

**Next step:**
Once quiz questions are provided (per `plan.md`'s pending content list), build the quiz page and wire `PinScreen`'s `onUnlock` to route there instead of the placeholder.

## Entry 3 — 2026-08-13 — added wish-input step to the plan (no code yet)

**Done:**
- User requested a new closing step: after the reward reveal, she types a birthday wish for herself, which gets emailed to the user via Formspree. Confirmed placement (after reward reveal, not after photo/music), storage approach (emailed via Formspree, not saved/displayed anywhere else), and content intent (a wish for herself, not a message to the user) via clarifying questions before touching anything, per this project's ground rules.
- Updated `plan.md`: flow list, a new numbered flow step (6, "Wish input"), a new stack exception noting Formspree is the one external network call in an otherwise fully static/no-backend project, two new pending-content items (Formspree endpoint URL, copy/prompt wording for the screen), and a new build-order step (6, blocked on the Formspree endpoint).
- **No code written this entry** — purely a planning update.

**Current state relative to build order:**
- Step 1 (PIN page) — done.
- Steps 2–5 — not started.
- Step 6 (wish input) — newly added to the plan, blocked on user providing a Formspree account/endpoint and exact copy for the screen.

**Unfinished / partial:**
- Nothing code-wise changed; this entry is plan-only.

**Open questions for user (not blocking):**
- Formspree endpoint URL — needed before step 6 can be built.
- Exact wording for the wish-input prompt and the post-submit confirmation message.
- (Carried over) hosting target, quiz questions, photos, song mp3, reward details.

**Next step:**
Same as Entry 2 — quiz page is still the next thing to build once quiz questions arrive. Wish-input step (6) can be built independently once its Formspree endpoint is available; it doesn't block anything upstream.

## Entry 4 — 2026-08-13 — added post-wish animation spec to the plan (no code yet)

**Done:**
- User proposed a post-submit animation for the wish-input step using anime.js — a "Your wish shall be granted" reveal with stars. Confirmed exact copy (typo fixed to "granted"), star style (twinkling starfield behind the text, not a burst), and end behavior (auto-settles after a few seconds, no tap needed since it's the final step) via clarifying questions.
- Updated `plan.md`: step 6's description now includes the animation spec; added anime.js to the Stack section as a new dependency; trimmed the pending-content list since the post-submit copy is now confirmed (only the wish-prompt copy and the Formspree endpoint remain outstanding).
- **No code written this entry** — still plan-only, since step 6 as a whole remains blocked on the Formspree endpoint and prompt copy.

**Current state relative to build order:**
- Unchanged from Entry 3: step 1 done, steps 2–5 not started, step 6 fully specified in the plan now but not built.

**Unfinished / partial:**
- Nothing code-wise; plan-only entry.

**Open questions for user (not blocking):**
- Formspree endpoint URL — still needed before step 6 can be built.
- Exact wording for the wish-input prompt (the box she types into) — still open. Post-submit line is now settled: "Your wish shall be granted."
- (Carried over) hosting target, quiz questions, photos, song mp3, reward details.

**Next step:**
Unchanged — quiz page is next once quiz questions arrive. Step 6 (wish input + animation) is ready to build in full as soon as the Formspree endpoint and wish-prompt copy come in.

## Entry 5 — 2026-08-13 — Formspree endpoint recorded

**Done:**
- User registered Formspree and provided the form endpoint: `https://formspree.io/f/mjybrpwa`. Recorded it in `plan.md`'s "Confirmed content" section (alongside PIN/hints/song) so it's there when step 6 gets built. Removed it from the pending-content checklist accordingly.
- Reminded user (in-chat, not written elsewhere): Formspree sends a one-time confirmation email to the recipient address the first time a form is created — worth checking that got verified, or submissions won't deliver.
- **No code written this entry** — plan-only update.

**Current state relative to build order:**
- Step 6 (wish input) now only blocked on one thing: the wish-prompt copy (what the text box asks her to write). Everything else for that step — endpoint, post-submit copy, animation spec — is settled in `plan.md`.
- Steps 2–5 still not started; quiz page remains the next actual build task once quiz questions arrive.

**Unfinished / partial:**
- Nothing code-wise; plan-only entry.

**Open questions for user (not blocking):**
- Wish-prompt copy — still open, last thing needed to build step 6 in full.
- (Carried over) hosting target, quiz questions, photos, song mp3, reward details.

**Next step:**
Unchanged — quiz page is next once quiz questions arrive. Step 6 can now be built in full the moment wish-prompt copy is provided.

## Entry 6 — 2026-08-13 — wish-prompt copy confirmed; step 6 fully specified

**Done:**
- Verified the Formspree side is actually ready: walked user through the dashboard, confirmed the form endpoint matches (`mjybrpwa`) and the recipient email `poomfail2013@gmail.com` is listed as VERIFIED under Settings → Linked Emails. The earlier "missing confirmation email" concern is a non-issue — the address is already verified, no action needed.
- User provided the wish-prompt copy (Thai): "ถ้าขอพรอะไรก็ได้ในวันเกิดปีนี้ อ้วนอยากจะขออะไร". Recorded in `plan.md`'s Confirmed content section; removed the last pending item for step 6.
- Updated `plan.md`'s build order: step 6 now reads "all content confirmed, ready to build" (previously blocked on this copy).
- **No code written this entry** — still plan-only. Have not yet asked/confirmed whether to start building step 6 now or continue waiting on other pending items (quiz questions, photos, song, reward details) first.

**Current state relative to build order:**
- Step 1 (PIN page) — done.
- Step 6 (wish input + animation) — fully specified in `plan.md`, no code written yet.
- Steps 2–5 — not started, still blocked on quiz questions / photos / song mp3 / reward details from user.

**Unfinished / partial:**
- Step 6 has zero code despite being fully specified — it was defined out of build order (added as a late request) and building it now would mean jumping ahead of steps 2–5 in the stated build order. Have not asked user whether that's desired.

**Open questions for user (not blocking):**
- Should step 6 be built now (out of order, since it's the only fully-unblocked step), or held until steps 2–5 are reached in sequence?
- (Carried over) hosting target, quiz questions, photos, song mp3, reward details.

**Next step:**
Ask the user whether to build the wish-input step (6) now since it's fully unblocked, or wait for quiz questions to build step 2 next per the stated build order.

**Resolution:** asked; user chose to wait for the queue (build in stated order, not out of order). Step 6 stays fully specified but unbuilt until steps 2–5 are reached. Next actual build task remains the quiz page (step 2), blocked on quiz questions from user.

## Entry 7 — 2026-08-13 — asset drop folder convention recorded

**Done:**
- User asked to record where they'll place source images/audio: a `source_file/` folder at project root. Added an "Asset locations" section to `plan.md`.
- Caught a naming mismatch while creating the folders: user said "Audio and Image folder," but `source_file/Audio/` and `source_file/images/` (lowercase, plural) already existed from before this conversation — I'd initially created a conflicting `source_file/Image/` (capital, singular) to match the literal wording. Flagged it instead of guessing; user confirmed to keep the pre-existing `images/` naming. Deleted the duplicate `Image/` folder and wrote `plan.md` to match what actually exists on disk: `source_file/images/` and `source_file/Audio/`.
- Both folders currently empty, ready for user to drop files in.
- **No code written this entry.**

**Current state relative to build order:**
- Unchanged — step 1 done, step 6 fully specified but unbuilt, steps 2–5 blocked on content from user. This entry just adds a defined drop location for photos/audio once step 3/4 are reached.

**Unfinished / partial:**
- Nothing partial — folder convention is settled and matches disk state.

**Open questions for user (not blocking):**
- (Carried over) hosting target, quiz questions, photos (now know they'll land in `source_file/images/`), song mp3 (→ `source_file/Audio/`), reward details.

**Next step:**
Unchanged — quiz page (step 2) is next once quiz questions arrive. When photos/song file are dropped into `source_file/images/` and `source_file/Audio/`, that unblocks steps 3 and 4 respectively.

## Entry 8 — 2026-08-13 — Questions.md created; song mp3 confirmed present

**Done:**
- Confirmed `Boys Like Girls - Two Is Better Than One.mp3` is present in `source_file/Audio/` (5.8 MB), matching the song already confirmed in `plan.md`. Unblocks step 4 whenever the build order gets there.
- User asked to record that quiz questions will be drafted in `Questions.md` and asked me to create that file. Created `Questions.md` at the project root with a simple template (one block per question: question text, 4 lettered options, correct answer), 3 blank blocks to start — user can add/remove blocks freely.
- Updated `plan.md`'s pending-content line for quiz questions to point at `Questions.md`.
- **No app code written this entry** — template file + plan update only.

**Current state relative to build order:**
- Unchanged — step 1 done, step 6 fully specified but unbuilt, steps 2/3/5 still blocked on content. Step 4 (music) is now content-unblocked (file present) but not yet built since it's later in the sequence.

**Unfinished / partial:**
- `Questions.md` is currently just an empty template — waiting on user to fill it in.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos (→ `source_file/images/`), reward details.

**Next step:**
Once `Questions.md` is filled in, build the quiz page (step 2) from it.

## Entry 9 — 2026-08-13 — quiz choice-order randomization recorded

**Done:**
- User asked to record that answer-choice order should be randomized/shuffled each time a question is opened or the page refreshed, rather than fixed. Added to `plan.md`'s Flow section (step 2 description).
- **No code written this entry** — plan-only update, quiz page still unbuilt pending `Questions.md` content.

**Current state relative to build order:**
- Unchanged — step 1 done, steps 2/3/5 blocked on content, step 4 content-ready but unbuilt, step 6 fully specified but unbuilt (held for build-order sequencing per user's earlier choice).

**Unfinished / partial:**
- Nothing partial — this was a single spec addition.

**Open questions for user (not blocking):**
- (Carried over) hosting target, quiz questions in `Questions.md`, photos, reward details.

**Next step:**
Unchanged — once `Questions.md` is filled in, build the quiz page (step 2), including per-load shuffled choice order.

## Entry 10 — 2026-08-13 — mobile on-screen keypad added to PIN page

**Done:**
- User asked for a mobile-shown keypad on the PIN screen. Flagged (per user's ask) that I could use a library but recommended building a simple custom one instead — matches the theme exactly and avoids a dependency; user didn't object, proceeded with custom build.
- Clarified scope via questions: keypad shows **mobile only** (detected via `matchMedia('(pointer: coarse)')`), and once shown, the 4 digit boxes become **read-only / keypad-only** input (no native keyboard popping up alongside it). Desktop is unaffected — boxes stay typeable via physical keyboard as before.
- Built `app/components/Keypad.tsx` — a 3-column grid (1–9, 0, backspace) styled with the existing wine/gold/blush/ivory tokens.
- Updated `app/components/PinScreen.tsx`: added mobile detection via a `matchMedia` listener, digit boxes become `readOnly` on mobile, keypad renders below them on mobile only, added `handleKeypadDigit`/`handleKeypadBackspace` that fill/clear the first-empty/last-filled box respectively (same PIN-check logic as before, unchanged).
- Recorded the behavior in `plan.md`'s design tokens section as a new "PIN entry input method" line.
- Verified: `tsc --noEmit` clean, dev server compiles with no errors, homepage renders PIN screen correctly. Killed a leftover dev server process from an earlier session in the process (PID 17892, unrelated stale process, not a new issue) and left everything stopped after verification.

**Current state relative to build order:**
- Step 1 (PIN page) — still done, now with mobile keypad enhancement. Steps 2–6 unchanged from prior entries (2/3/5 blocked on content, 4 content-ready, 6 fully specified, all unbuilt).

**Unfinished / partial:**
- Nothing partial — mobile keypad feature is complete and verified. Not tested on an actual physical mobile device/emulator (only verified via code review + `pointer: coarse` logic + successful compile), so worth a real device check before calling it fully done.

**Open questions for user (not blocking):**
- (Carried over) hosting target, quiz questions in `Questions.md`, photos, reward details.

**Next step:**
Unchanged — once `Questions.md` is filled in, build the quiz page (step 2). Worth a real mobile device test of the new keypad next time user is near one.

## Entry 11 — 2026-08-13 — fixed hint reveal layout shift

**Done:**
- User reported (with a mobile screenshot) that each new hint appearing pushed the PIN boxes and keypad upward, since the hints container only existed in the DOM once revealed, growing the centered flex layout.
- Fix in `app/components/PinScreen.tsx`: all 3 hint `<p>` elements now always render, using `invisible` (Tailwind, keeps layout space but hides visually) for hints not yet shown, and the existing `animate-soft-reveal` class for ones that are. Space for all 3 hints is reserved from first load, so revealing one now just fades it in without moving anything else.
- Updated `plan.md` with a "Hint layout" line documenting this.
- Verified: `tsc --noEmit` clean, dev server compiles with no errors (had to clean up another stale leftover dev server process again — same recurring minor annoyance as Entry 10, not a real issue, just note to self to kill node processes at start of session if this keeps happening).

**Current state relative to build order:**
- Step 1 (PIN page) — done, now with both mobile keypad and fixed hint layout. Steps 2–6 unchanged from prior entries.

**Unfinished / partial:**
- Nothing partial. Not yet re-verified on an actual mobile device/screenshot after this fix (only verified via code + compile) — worth confirming visually next time user has mobile access, same as the Entry 10 keypad caveat.

**Open questions for user (not blocking):**
- (Carried over) hosting target, quiz questions in `Questions.md`, photos, reward details.

**Next step:**
Unchanged — once `Questions.md` is filled in, build the quiz page (step 2).

## Entry 12 — 2026-08-13 — dramatic answer-reveal spec added; wrong-answer flow decision resolved

**Done:**
- User asked for a dramatic reveal after tapping a quiz answer. Clarified via questions: exact Thai text for each case, effect style, and resolved a previously-open decision from `plan.md` (whether wrong answers block progress) at the same time.
- Confirmed: **wrong answers shake the screen** and show "เป็นคำตอบที่ยังม่ายช่ายยยย"; **correct answers get a sparkle/glow effect** (not a shake — asymmetric, shake reads as error-only) and show "เป็นคำตอบที่ถูกต้องนะค้าบบบบ". Wrong answers **block progress** — she retries the same question until correct before advancing.
- Updated `plan.md`: step 2's flow description now includes the full reveal spec; removed the "whether wrong answers block progress" line from Open technical/product decisions since it's now resolved.
- Also caught and fixed a stale line while editing that section: the pending-content checklist still listed "Song mp3 file upload" as outstanding, but that file was confirmed present in `source_file/Audio/` back in Entry 8 — removed it.
- **No code written this entry** — quiz page itself is still unbuilt, blocked on `Questions.md` content. This just adds to its spec for whenever it's built.

**Current state relative to build order:**
- Unchanged — step 1 done, step 2 now more fully specified but still blocked on `Questions.md`, steps 3/5 blocked on content, step 4 content-ready, step 6 fully specified and held for sequencing.

**Unfinished / partial:**
- Nothing partial — spec addition only.

**Open questions for user (not blocking):**
- (Carried over) hosting target, quiz questions in `Questions.md`, photos, reward details.

**Next step:**
Unchanged — once `Questions.md` is filled in, build the quiz page (step 2) including the shuffled choices, block-until-correct flow, and the shake/sparkle reveal with the confirmed Thai copy.

## Entry 13 — 2026-08-13 — quiz page built

**Done:**
- User filled in `Questions.md` with 3 questions (Thai, "about us" theme). Built the quiz page from it.
- Built `app/components/QuizScreen.tsx`: questions/options hardcoded from `Questions.md` content, choices re-shuffled (Fisher-Yates) every time a question loads (satisfies the "shuffled every open/refresh" spec from Entry 9). Tapping an option shows the dramatic reveal from Entry 12 — correct answers get the new sparkle/glow effect (gold pulsing box-shadow) plus "เป็นคำตอบที่ถูกต้องนะค้าบบบบ" and auto-advance to the next question (or call `onComplete` on the last one); wrong answers shake the whole screen plus "เป็นคำตอบที่ยังม่ายช่ายยยย", then reset after a beat so she can retry the same question — no advancing on a wrong pick, per the block-until-correct decision.
- Added a `sparkle` keyframe + `.animate-sparkle` class to `app/globals.css` for the correct-answer glow (pure CSS, no new dependency — anime.js stays reserved for the wish-input step per plan.md).
- Reused the Entry 11 lesson proactively: the reveal-text line always renders (reserving its space) and just toggles `invisible`, so showing feedback text doesn't shift the layout the way the PIN hints originally did.
- Wired into `app/page.tsx`: PIN unlock → `QuizScreen` → on quiz complete, a placeholder "Quiz complete ✦ / Photo album step coming next" screen (photo album itself isn't built yet — out of scope for this step, same pattern as the earlier PIN→quiz placeholder).
- Updated `plan.md`: marked quiz questions off the pending-content list (now sourced from `Questions.md` directly in code), removed the "exact quiz question count/format" line from open decisions (resolved — 3 questions, now built), marked build-order step 2 as done.
- Verified: `tsc --noEmit` clean, dev server compiles with no errors, homepage returns 200. Cleaned up stray leftover node processes before and after (recurring minor housekeeping, not a new issue). Dev server stopped after verification.

**Current state relative to build order:**
- Steps 1–2 — **done**. Step 3 (photo album) is next, blocked on photos in `source_file/images/`. Step 4 (music) content-ready but unbuilt. Step 5 blocked on reward details. Step 6 fully specified but held for sequencing per user's earlier choice.

**Unfinished / partial:**
- Quiz flow only verified via compile + code review, not a real click-through in a browser by a human — logic looks correct (shuffle, block-on-wrong, advance-on-correct) but worth a manual pass whenever convenient.
- `page.tsx`'s post-quiz placeholder is a stand-in until the photo album is built.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos (→ `source_file/images/`), reward details.

**Next step:**
Photo album (step 3) is next once photos are dropped into `source_file/images/`.

## Entry 14 — 2026-08-13 — photo album organization spec added (no code yet)

**Done:**
- User proposed organizing the photo album by year, with a `source_file/images/2022/`, `2023/`, etc. subfolder structure. Clarified two details before recording: photos within each year sort by filename (user will name files `01.jpg`, `02.jpg`, ... to control order), and yes to a styled divider page showing just the year number before each year's first photo.
- Updated `plan.md`: added the year-subfolder/filename-order convention to the Asset locations section, and expanded step 3's flow description with the year-grouping + divider-page spec.
- **No code written this entry** — photo album (step 3) is still unbuilt, blocked on actual photos being dropped into `source_file/images/`. This just locks in the structure for whenever they arrive.

**Current state relative to build order:**
- Unchanged — steps 1–2 done, step 3 now more fully specified but still blocked on photo content, step 4 content-ready, step 5 blocked on reward details, step 6 fully specified but held for sequencing.

**Unfinished / partial:**
- Nothing partial — spec addition only.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos (→ `source_file/images/2022/`, `2023/`, etc.), reward details.

**Next step:**
Unchanged — once photos land in `source_file/images/` (organized by year subfolder, filenames controlling order), build the photo album (step 3) with StPageFlip, year dividers, and the hybrid auto-flip/manual-flip behavior.

## Entry 15 — 2026-08-13 — corrected photo album layout: one page per year, not one page per photo

**Done:**
- User corrected a misunderstanding from Entry 14: it's **one page per year** with all of that year's photos together on it, not one page per individual photo with a separate year-divider page. Clarified the resulting layout questions: photos on a year's page use a **scattered/collage** arrangement (slight overlap/rotation, scrapbook feel — not a uniform grid), and if a year has too many photos to fit well, it **spills onto an additional page** for that year rather than the user trimming photos down.
- Corrected `plan.md`'s step 3 description accordingly — replaced the "divider page before each year" concept (no longer applies now that the year page itself holds the photos) with the collage-per-year-page spec, year number shown directly on that page, and overflow-to-second-page behavior.
- **No code written this entry** — still spec-only, photo album remains blocked on actual photos in `source_file/images/`.

**Current state relative to build order:**
- Unchanged — steps 1–2 done, step 3 spec corrected but still blocked on photo content, step 4 content-ready, step 5 blocked on reward details, step 6 fully specified but held for sequencing.

**Unfinished / partial:**
- Nothing partial — this was purely a spec correction.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos (→ `source_file/images/2022/`, `2023/`, etc.), reward details.
- Not yet asked: roughly how many photos per year should be expected, to gauge how dense the collage layout needs to handle (not blocking — can adapt once real photos arrive).

**Next step:**
Unchanged — once photos land in `source_file/images/`, build the photo album (step 3): one collage page per year, year number on the page, overflow to a second page per year if needed.

## Entry 16 — 2026-08-13 — photo entrance animation spec added (no code yet)

**Done:**
- User asked for photos to fly in and get pinned onto the page. Clarified: triggers **every time** that year's page is flipped to (not just first view, replays on repeat visits), **staggered** entrance (one photo after another, not all at once), and each photo gets a **visual pushpin/washi-tape-corner graphic** once it lands (not just implied by the settling motion — an actual decorative element).
- Updated `plan.md`'s step 3 description with a new "Entrance animation" sub-line covering all three.
- **No code written this entry** — photo album is still unbuilt, blocked on actual photos in `source_file/images/`. This adds to its spec for whenever it's built.

**Current state relative to build order:**
- Unchanged — steps 1–2 done, step 3 spec further expanded but still blocked on photo content, step 4 content-ready, step 5 blocked on reward details, step 6 fully specified but held for sequencing.

**Unfinished / partial:**
- Nothing partial — spec addition only.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos (→ `source_file/images/2022/`, `2023/`, etc.), reward details.
- Still open from Entry 15: roughly how many photos per year, to gauge collage density — not blocking.

**Next step:**
Unchanged — once photos land in `source_file/images/`, build the photo album (step 3) with the full spec now assembled: year-grouped collage pages, staggered fly-in-and-pin entrance with pushpin/tape graphics on every visit to that page, overflow handling, and StPageFlip's hybrid auto/manual flip.

## Entry 17 — 2026-08-13 — photo album prototype built with placeholder photos

**Done:**
- User asked to build the photo album now using picsum.photos placeholder images, specifically to preview the fly-in/pin motion before real photos arrive. Agreed this was worth doing ahead of real content — same reasoning as building against `Questions.md`/Formspree earlier: build against a stand-in, swap real content in later without touching logic.
- Installed `page-flip` (npm distribution of StPageFlip) — added to `plan.md`'s Stack section. The package ships no TypeScript types, so added a minimal shim at `app/types/page-flip.d.ts` covering only what's used (`PageFlip` class, `loadFromHTML`, `on('flip', ...)`, `destroy`). Also inlined the package's core CSS (`.stf__parent`/`.stf__block`/etc.) directly into `app/globals.css` since the npm package doesn't publish a resolvable dist CSS file — sourced from `node_modules/page-flip/src/Style/stPageFlip.css`.
- Built `app/components/YearPage.tsx` — renders one year's photos in a fixed set of 8 hand-tuned scatter slots (position + rotation) for a scrapbook collage look, cycling through slots if a year has more photos than 8 (real overflow-to-second-page logic still TODO, see below). Each photo has a small gold "pushpin" dot and plays the fly-in-pin entrance animation, staggered via `animationDelay`.
- Added `fly-in-pin` keyframe to `globals.css`: photos fly in from off-screen (upper-left), overshoot slightly past their final rotation, then settle — reads as "flying in and getting pinned."
- Built `app/components/PhotoAlbum.tsx` — dynamically imports `page-flip` (client-only, avoids SSR issues), initializes a `PageFlip` instance over two placeholder year pages (2022: 5 photos, 2023: 6 photos, both `picsum.photos/seed/...` URLs). Listens for the library's `flip` event and bumps a per-page `animKey`, which remounts that page's photos (via React `key`) to replay the fly-in-pin animation — satisfies the "replay every time flipped to" spec from Entry 16.
- Wired into `app/page.tsx`: quiz complete → `PhotoAlbum` (replacing the earlier placeholder text screen).
- Verified: `tsc --noEmit` clean (including the new type shim), dev server compiles with no errors, homepage (PIN screen) still returns 200. **Not verified**: actually clicking through PIN → quiz → album in a live browser to confirm the flip mechanics and fly-in animation work as intended at runtime — only static compile + code review. Worth a manual click-through before trusting this fully.
- What's explicitly NOT done yet: auto-flip-on-timer (plan says "hybrid mode: auto-flips on a timer but she can also swipe/tap") — this prototype is manual-flip only for now; year-divider/overflow-to-second-page logic when a year exceeds 8 photos; swapping placeholder picsum photos for real ones.

**Current state relative to build order:**
- Steps 1–2 done. Step 3 (photo album) has a working prototype with placeholder content — page-flip + collage + fly-in-pin animation all wired up, but auto-flip timer and overflow handling are still missing, and real photos aren't in yet. Steps 4–6 unchanged (4 content-ready, 5 blocked on reward details, 6 fully specified but held).

**Unfinished / partial:**
- Auto-flip-on-timer not implemented (manual flip only right now).
- Overflow-to-second-page-per-year logic not implemented (currently just cycles through 8 fixed slots, which would visually overlap badly past 8 photos rather than spilling to a new page).
- Not manually click-tested in a live browser — only compiled/reviewed.
- Real photos still not swapped in (waiting on user to populate `source_file/images/2022/`, `2023/`, etc.).

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos, reward details.
- Does the fly-in-pin motion feel right once you preview it? (the actual point of this entry — please try it and give feedback)

**Next step:**
Get user feedback on the placeholder preview (motion feel, collage layout, flip mechanics). Then: implement auto-flip timer + overflow-to-second-page logic, and once real photos land in `source_file/images/`, swap them in for the picsum placeholders.

## Entry 18 — 2026-08-13 — beat-synced fly-in + bigger photos; music playback pulled forward into step 3

**Done:**
- User asked for two things: bigger photos (1.5x) and the fly-in animation to actually follow the song's rhythm. The rhythm request meant pulling real audio playback into the photo album now (originally step 4, later in build order) — clarified via questions that this should be genuine beat-detection (not just "feels slower"), that I'd handle timing via automatic real-time analysis rather than manually authored timestamps (can't listen to the mp3 myself to hand-mark beats), and that the song should actually start playing now rather than staying silent.
- Copied the song from `source_file/Audio/` into `public/audio/two-is-better-than-one.mp3` (files outside `public/` aren't servable by Next.js).
- `YearPage.tsx`: photos enlarged from `h-28 w-24` to `h-[168px] w-36` (exactly 1.5x). Replaced the old CSS `animationDelay`-based stagger with a `visibleCount` prop — photos only render once beat-driven logic in the parent says they should, so entrance timing is actually driven by external state, not baked into CSS.
- `globals.css`: slowed the `fly-in-pin` keyframe from 0.7s to 1.3s with a softer easing curve, per "fly in slower."
- `PhotoAlbum.tsx` rewritten with: an `<audio>` element (loops, autoplay attempted on mount), a Web Audio API `AnalyserNode` reading bass-frequency energy every animation frame, and simple energy-threshold onset detection (energy > 1.35x running average, above a floor, at least 320ms since the last detected beat) incrementing a `beatCount` state on each detected beat. Each page tracks the `beatCount` value at the moment it was activated (flipped to); a page's photos reveal one-by-one as `beatCount` advances past that baseline — first photo always shows immediately so the page is never empty if beat detection stalls. This is energy-based onset detection, not real music-information-retrieval beat-tracking — good enough to feel rhythmic, not scientifically precise.
- **Bug caught via actual browser runtime, not just static compile**: the dev log captured a real `[browser] Uncaught InvalidStateError` — `createMediaElementSource` can only be called once ever per `<audio>` element (even across a fresh `AudioContext`), and React Strict Mode's dev-mode double-effect-invocation was calling it twice. Fixed by caching the analyser in a ref so the source node is created only once and reused across the extra Strict Mode invocation. Re-verified after the fix: same dev-log capture showed a clean run with no console errors this time — this is a genuine runtime verification (via the harness's browser console capture), not just `tsc`/compile success like most earlier entries.
- Updated `plan.md`: step 3 now documents 1.5x photo size and beat-driven (not timer-driven) staggering; step 4 marked "basic playback done" with the autoplay-reliability caveat called out explicitly; Stack section doesn't need a new dependency line since Web Audio API is a browser built-in, not a package.
- **Explicitly not done**: real-device autoplay testing (iOS Safari in particular is strict about this and may block playback despite earlier interaction in the PIN/quiz steps — if that happens, the album still works, just silently and without beat-sync until she taps something). Auto-flip timer and overflow-to-second-page (still open from Entry 17) remain unbuilt.

**Current state relative to build order:**
- Steps 1–2 done. Step 3 has beat-synced animation + page-flip working with placeholder photos. Step 4 (music) has basic real playback/looping done, pulled forward — still needs a real-device check and any further polish. Steps 5–6 unchanged.

**Unfinished / partial:**
- Autoplay reliability unverified on a real phone.
- Auto-flip-on-timer and overflow-to-second-page-per-year still not implemented (carried over from Entry 17).
- Real photos still not swapped in.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos, reward details.
- Does the beat-synced timing actually feel right when you try it? Energy-threshold detection is an approximation, not precise beat-tracking — may need tuning (the `MIN_BEAT_INTERVAL_MS` / `ENERGY_THRESHOLD_MULT` constants in `PhotoAlbum.tsx`) once heard against the real song.

**Next step:**
User to try it live (ideally with sound on) and give feedback on both the size/pacing changes and whether the beat-sync feels convincing. Then: real-device autoplay check, auto-flip timer, overflow handling, and swapping in real photos.

## Entry 19 — 2026-08-13 — fixed photo/year-label overlap

**Done:**
- User reported photos colliding with the year number text at the top of each page (the scatter slots started near the very top, same area as the year label).
- Restructured `YearPage.tsx`'s layout: year label now sits in its own fixed-height header row (`h-12` flex row) at the top of the page; photos scatter inside a separate `flex-1` container below it. Since the scatter positions are percentages relative to their own container (not the whole page), this guarantees no overlap regardless of slot values — didn't need to renumber the SLOTS percentages, just moved them into the right containing box.
- Also switched `h-[168px]` to Tailwind's canonical `h-42` per an IDE lint suggestion (Tailwind v4 supports arbitrary integer spacing steps directly) — same value, cleaner class.
- Verified: `tsc --noEmit` clean, dev server compiles with no console errors.

**Current state relative to build order:**
- Unchanged — steps 1–2 done, step 3 layout bug fixed (still placeholder photos), step 4 basic playback done, steps 5–6 unchanged.

**Unfinished / partial:**
- Same carried-over items as Entry 18: real-device autoplay check, auto-flip timer, overflow-to-second-page, real photos not yet swapped in.
- Layout fix verified via compile only, not a live visual check — worth confirming the header/photo split looks right once user tries it.

**Open questions for user (not blocking):**
- (Carried over) hosting target, photos, reward details, feedback on beat-sync feel from Entry 18.

**Next step:**
Unchanged from Entry 18 — awaiting user's live feedback, then real-device autoplay check, auto-flip timer, overflow handling, and real photos.

## Entry 20 — 2026-08-13 — reward reveal fully specified (no code yet)

**Done:**
- User proposed the reward reveal concept: a present box she taps to open, one ticket pops out and spins 2–3 times, glitter/stars, animated with anime.js (reusing the dependency already introduced for step 6 — no new library). Clarified trigger (tap/click, not auto-open) and ticket count (1, not 2) via questions.
- Content took a few rounds to pin down: initially picked "something else" over a trip, then "an event or date," then the actual reward turned out to be a "do whatever she wants" pass rather than a specific dated event — title "บัตรตามใจ", with a subtitle confirmed as "สามารถใช้บัตรใบนี้เพื่อให้เราตาใจได้ บัตรใบนี้มีผล 24 ชั่วโมง วันหมดอายุ:ไม่มี" (valid 24 hours, no expiry date). User said text styling is open to my judgment.
- Updated `plan.md`: step 5's flow description now has the full mechanic + copy; added the reward ticket copy to Confirmed content; removed the reward-details line from Pending content (now empty — nothing left pending except photos) and removed the "reward reveal format" line from Open technical/product decisions (now resolved); marked build-order step 5 as content-confirmed/ready to build.
- **No code written this entry** — spec and content only. Photo album (step 3) is still the active build item awaiting user's live feedback; reward reveal (step 5) is now unblocked content-wise but comes later in the build order.

**Current state relative to build order:**
- Steps 1–2 done. Step 3 has a working prototype awaiting feedback. Step 4 has basic playback done. Step 5 now fully specified, content-ready, unbuilt. Step 6 fully specified, held for sequencing.
- Only remaining pending-content item across the whole plan: photos for the album.

**Unfinished / partial:**
- Nothing partial — spec/content entry only.

**Open questions for user (not blocking):**
- Hosting/deployment target — last open technical decision in the whole plan.
- Photos for the album — last open content item.
- (Carried over) live feedback on the photo album preview from Entries 18–19.

**Next step:**
Still waiting on user's feedback on the photo album preview before continuing step 3 polish (auto-flip timer, overflow handling). Reward reveal (step 5) can be built whenever its turn comes up in the build order — all content is ready.

## Entry 21 — 2026-08-13 — reward reveal (step 5) built

**Done:**
- User said to proceed with step 5 now (ahead of finishing step 3 polish/feedback), so built it.
- Installed `animejs` (v4 — modular API: `createTimeline`, `stagger`, `utils`, not the older v3 `anime()` single-function API). No published TypeScript issues — v4 ships its own `.d.ts` files, no shim needed like `page-flip`.
- Built `app/components/RewardReveal.tsx`: a present box (CSS divs, wine/gold, lid + base + ribbon) sits on screen; tapping it plays an animejs timeline — lid rotates open, the ticket (border-dashed card reading "บัตรตามใจ" / the confirmed subtitle) scales up from inside the box while spinning 3 full turns (`rotate: [0, 1080]`) with an elastic ease, glitter star characters (✦/✧) burst outward with staggered timing and fade, then the ticket settles with a small bounce. Runs once per visit (`opened` state disables the button after).
- Caught and fixed my own mistake mid-build: first draft had a stray, nonsensical `animate(tl, {})` call left over from second-guessing the API — `createTimeline()` autoplays on its own; removed the dead call and the unused import before finishing.
- Wired into the flow: `PhotoAlbum` now takes an `onContinue` prop and shows a "Continue to your surprise →" link below the flip hint — a **deliberately temporary stand-in**, since the album doesn't yet have a real "finished" moment (auto-flip timer / last-page detection are still unbuilt from Entry 17). This just makes step 5 reachable/testable now. `page.tsx`: PIN → quiz → album → (continue link) → `RewardReveal`.
- Verified: `tsc --noEmit` clean (including animejs's v4 types), dev server compiles with no console errors.
- **Not verified**: an actual click-through of the box-open animation in a live browser — only static compile + code review, same caveat pattern as the photo album build in Entry 17.

**Current state relative to build order:**
- Steps 1, 2, 5 done. Step 3 has a working prototype (unchanged, still awaiting feedback). Step 4 has basic playback done. Step 6 fully specified, still held.
- Full flow is now click-through-able end to end for the first time: PIN → quiz → album (placeholder photos) → tap-through to reward → open the box.

**Unfinished / partial:**
- The "Continue to your surprise" link on the album is scaffolding, not final — needs replacing once step 3's real completion logic (auto-flip timer, last page reached, etc.) is built.
- Reward animation not manually click-tested in a live browser.
- Real photos, hosting target still outstanding (last two open items in the whole plan, per Entry 20).

**Open questions for user (not blocking):**
- Does the box-open/spin/glitter sequence feel right once tried live? Timing/easing values are first-pass guesses (`RewardReveal.tsx`), easy to tune.
- (Carried over) live feedback on the photo album preview, real photos, hosting target.

**Next step:**
User to try the reward reveal (and the rest of the flow) live. Then: build step 3's real completion logic to replace the temporary "Continue" link, auto-flip timer, overflow handling, and swap in real photos once available.

## Entry 22 — 2026-08-13 — fixed intermittent box-tap bug on reward reveal

**Done:**
- User reported the present box sometimes not responding to a tap. Root cause: the ticket card sits visually on top of the box (`z-10`, absolutely positioned, overlapping) and stays at `opacity-0` until opened — but opacity alone doesn't disable pointer events, so the invisible ticket rectangle was still catching clicks. Wherever it overlapped the box, taps landed on the (inert) ticket div instead of the button underneath and silently did nothing; taps outside that overlap worked fine. Matches exactly the "sometimes works, sometimes doesn't, depending on where you tap" symptom.
- Fix in `app/components/RewardReveal.tsx`: added `pointer-events-none` to the ticket's wrapper div (it doesn't need to be interactive — it's revealed by animation, not tapped). The glitter wrapper already had this correctly set from the start; the ticket was the one gap.
- Verified: `tsc --noEmit` clean, dev server compiles with no console errors.
- **Not yet re-confirmed by the user tapping around the whole box** — the fix is a straightforward pointer-events correction (the kind of bug that's easy to be fully confident about from the CSS itself, not just circumstantial), but worth a quick real check since UI hit-testing bugs like this can occasionally have more than one contributing overlap.

**Current state relative to build order:**
- Unchanged — steps 1, 2, 5 done, step 3 prototype unchanged, step 4 basic playback done, step 6 held.

**Unfinished / partial:**
- Same carried-over items as Entry 21: temporary "Continue" link on the album, auto-flip timer, overflow handling, real photos, hosting target.

**Open questions for user (not blocking):**
- Please confirm the box now opens reliably wherever you tap it.
- (Carried over) feedback on the reward animation feel, photo album feedback, real photos, hosting target.

**Next step:**
Unchanged from Entry 21 — awaiting user's live feedback on both the reward reveal and the photo album, then step 3's real completion logic / auto-flip timer / overflow handling, and real photos.

## Entry 23 — 2026-08-13 — slowed ticket animation, fixed rotation axis

**Done:**
- User said the ticket appears too fast, and asked to fix the rotation "from vertical to horizontal." Clarified via a question that this meant the spin axis, not just speed: the previous `rotate` property does a flat, in-plane (Z-axis) spin, which on a non-square rectangle makes it visually tip onto its side at 90°/270° through each turn — confirmed the fix should be a proper vertical-axis (Y-axis) card-flip instead, which stays upright/readable at rest and never looks sideways.
- `RewardReveal.tsx`: swapped `rotate: [0, 1080]` for `rotateY: [0, 1080]` (still 3 full flips, now around the vertical center line — the existing `perspective: 800px` on the wrapping div already gives this proper 3D depth, no extra setup needed). Slowed the whole sequence down proportionally: lid open 450ms→600ms, ticket pop/flip 950ms→1700ms, glitter burst 750ms→900ms, settle bounce 450ms→550ms, with timeline offsets recalculated to keep the same relative overlap between stages.
- Verified: `tsc --noEmit` clean, dev server compiles with no console errors.
- **Not yet visually confirmed** — same caveat as prior animation work, changes are logically sound (correct anime.js v4 property names/timeline offsets) but not click-tested live by me.

**Current state relative to build order:**
- Unchanged — steps 1, 2, 5 done (5 now tuned), step 3 prototype unchanged, step 4 basic playback done, step 6 held.

**Unfinished / partial:**
- Same carried-over items as Entry 22: temporary "Continue" link on the album, auto-flip timer, overflow handling, real photos, hosting target.

**Open questions for user (not blocking):**
- Does the new pacing/flip axis feel right now? Exact duration numbers are still first-pass guesses, easy to nudge further.
- (Carried over) photo album feedback, real photos, hosting target.

**Next step:**
Unchanged — awaiting user's live feedback across the reward reveal and photo album, then step 3's completion logic / auto-flip timer / overflow handling, and real photos.

## Entry 24 — 2026-08-13 — slowed ticket spin further (0.5x)

**Done:**
- User said the spin was still too fast, asked for 0.5x of the current speed. Doubled the ticket's `rotateY` duration in `RewardReveal.tsx` (1700ms → 3400ms) and recalculated the glitter burst's timeline offset to keep the same proportional overlap into the now-longer spin (-1350 → -2700), so the glitter still bursts at the same relative point in the animation rather than firing too early/late against the slower spin.
- Verified: `tsc --noEmit` clean, dev server compiles with no console errors.
- Not visually confirmed live — same standing caveat as the last two animation-tuning entries.

**Current state relative to build order:**
- Unchanged — steps 1, 2, 5 done (5 still being tuned on request), step 3 prototype unchanged, step 4 basic playback done, step 6 held.

**Unfinished / partial:**
- Same carried-over items as Entry 23.

**Open questions for user (not blocking):**
- Does 0.5x speed feel right now, or does it need further adjustment either direction?
- (Carried over) photo album feedback, real photos, hosting target.

**Next step:**
Unchanged — awaiting user's live feedback, then step 3's completion logic / auto-flip timer / overflow handling, and real photos.

## Entry 26 — 2026-08-13 — reserved a drop location for a designed ticket image

**Note:** the user independently edited `RewardReveal.tsx` between Entry 25 and this entry — fixed a typo (ตาใจ → ตามใจ) and reformatted the subtitle onto its own line break. Preserved as-is, not reverted.

**Done:**
- User wants to design the "บัตรตามใจ" ticket as an image and send it later to replace the current text/border-dashed card in `RewardReveal.tsx`. Asked where it should go; created `source_file/ticket/` (new folder, following the same convention as `source_file/images/` and `source_file/Audio/`).
- Updated `plan.md`: noted the planned swap under the reward reveal's asset-locations entry, and added it to the pending-content list (marked optional/nice-to-have since the text version already works fine as a fallback).
- **No code written this entry** — just reserved the location. The actual swap happens once the image arrives.

**Current state relative to build order:**
- Unchanged — steps 1, 2, 5 done, step 3 prototype unchanged, step 4 basic playback done, step 6 held. Step 5 has an optional pending enhancement (ticket image) that doesn't block anything.

**Unfinished / partial:**
- Ticket image not yet provided — `source_file/ticket/` is empty, waiting on user.

**Open questions for user (not blocking):**
- (Carried over) live feedback on reward reveal animation, photo album feedback, real photos, hosting target.

**Next step:**
Unchanged — awaiting user's live feedback, then step 3's completion logic / auto-flip timer / overflow handling, real photos, and (whenever ready) the designed ticket image swap. — 2026-08-13 — added shining light burst + ambient falling stars to reward reveal

**Done:**
- User asked for a shining light burst when the box opens, and falling stars in the background once the ticket is shown. Said other libraries were fine if needed, but decided none were necessary — built both with what's already in place (CSS keyframes + the existing anime.js timeline), keeping the dependency list unchanged. Clarified via questions: warm golden glow (not white rays) for the light, and the falling stars should continue indefinitely in the background rather than stop after a few seconds.
- `globals.css`: added a `star-fall` keyframe (falls from above the viewport to below it, drifting sideways via a `--drift` CSS variable, fading in/out at the edges) with `animation-iteration-count: infinite` — each star loops on its own randomized timing.
- `RewardReveal.tsx`:
  - Added a `lightRef` div — a radial gold gradient circle, positioned at the box, added as the very first step in the anime.js timeline (flashes opacity 0→1→0 while scaling up) with the lid opening overlapped right after it so the light leads the reveal as asked.
  - Added a falling-stars background layer: 22 stars with randomized horizontal position, size, fall duration/delay, and sideways drift, generated once via `useMemo` (so they don't resync/restart on re-render) and only rendered once `opened` is true — matches "when the ticket is shown."
  - Added `relative`/`z-*` layering across the screen so the new full-screen star layer sits behind the box/ticket/text without interfering with clicks (kept `pointer-events-none` throughout, consistent with the Entry 22 fix).
- Verified: `tsc --noEmit` clean, dev server compiles with no console errors.
- Not visually confirmed live — same standing caveat as the rest of this component's animation work.

**Current state relative to build order:**
- Unchanged — steps 1, 2, 5 done (5 still being enhanced on request), step 3 prototype unchanged, step 4 basic playback done, step 6 held.

**Unfinished / partial:**
- Same carried-over items as Entry 24.

**Open questions for user (not blocking):**
- Does the light burst timing/color and the falling-star density/speed feel right once tried live? All first-pass values, easy to tune.
- (Carried over) photo album feedback, real photos, hosting target.

**Next step:**
Unchanged — awaiting user's live feedback, then step 3's completion logic / auto-flip timer / overflow handling, and real photos.

## Entry 27 — 2026-08-13 — photo album auto-flip timer + overflow-to-extra-page built

**Done:**
- User asked to build the auto-flip timer and overflow handling for the photo album (previously flagged as missing pieces of step 3).
- `YearPage.tsx`: exported `SLOTS_PER_PAGE` (=8, the number of hand-tuned scatter slots) so `PhotoAlbum.tsx` can chunk photos consistently; added optional `pageInYear`/`totalPagesInYear` props that render a small "1 / 2" style indicator under the year label when a year spans more than one page.
- `PhotoAlbum.tsx`:
  - Added `chunkIntoPages()` — splits each year's photo list into `SLOTS_PER_PAGE`-sized chunks, so a year with more than 8 photos now spills onto an additional page for that year (per plan.md) instead of losing photos or overcrowding one page. A year with zero photos still gets one empty page rather than vanishing.
  - Replaced the flat `YEARS.map(...)` render/state with `PAGES` (the chunked list) throughout — `animKeys`, `activationBeats`, and the beat-driven `visibleCount` logic are now indexed by page, not by year.
  - Added `currentIndex` state, updated from the page-flip library's `flip` event (previously only used to drive photo entrance, not tracked for its own sake).
  - Added the auto-flip timer itself: once the active page's last photo has finished flying in (`currentPageComplete`), a `AUTO_FLIP_DELAY_MS` (4.5s) timeout either calls `pageFlip.flipNext()` (more pages left) or `onContinue()` directly (last page overall) — this replaces the old placeholder "Continue to your surprise →" button, which is now removed. A manual swipe/tap changes `currentIndex`, which cancels/reschedules this effect against the newly active page — this is the "hybrid" behavior plan.md calls for (auto-advances, but she can go faster/slower manually anytime).
- Verified: `tsc --noEmit` clean; dev server starts, compiles, and serves `200` with no console errors in the log.
- Not verified: haven't manually driven the browser through the flip sequence, waited out the real timers, or tested the multi-page-per-year path with actual overflow (still on placeholder photos — 2022 has 5, 2023 has 6, so overflow chunking is exercised structurally but never actually produces a 2nd page yet since both years are under 8).

**Current state relative to build order:**
- Step 3 (photo album) is functionally complete pending real photos — page-flip mechanics, year collage, beat-synced fly-in, auto-flip timer, and overflow-to-extra-page are all built. Only placeholder picsum.photos images and no live-device confirmation remain before calling it done.
- Steps 1, 2, 5 unchanged (done). Step 4 unchanged (basic playback done, no real-device autoplay check yet). Step 6 unchanged (held, spec-complete).

**Unfinished / partial:**
- Real photos still needed to replace picsum.photos placeholders (biggest remaining blocker for step 3).
- Overflow path (a year with >8 photos) is written but never actually exercised yet — worth a real test once real photo counts are known.
- The album's reward-reveal handoff (`onContinue`) now fires automatically off a timer instead of a manual tap — worth the user trying live, since it's a behavior change (no more button to press).

**Open questions for user (not blocking):**
- Does 4.5s per page feel like the right pace once tried live, or should it be longer/shorter?
- (Carried over) live feedback on reward reveal, photo album collage/motion, real photos, hosting target.

**Next step:**
Try the album live (needs a page load through to step 3) to confirm auto-flip pacing and that manual swipe still works alongside it, then swap in real photos once provided. Ticket image and hosting target remain open whenever the user gets to them.

## Entry 28 — 2026-08-13 — added letter pages (step 7); built wish input (step 6) to wire the chain together

**Done:**
- User asked for a new step after the reward reveal: a button leading into 2-3 pages of a personal birthday message, 4-5 sentences per page, sentences appearing in order "like the image [photo] entrance." Clarified via questions across a few rounds:
  - Placement: at the very end of the whole flow, after wish input's existing starfield/end screen (not right after the reward reveal as first suggested).
  - Sentence entrance style: typewriter/line-by-line fade-in, not the photo album's fly-in-pin.
  - Navigation: same page-flip book mechanics as the photo album.
  - Content: placeholder text for now, real message to be dropped in later.
  - The wish-input end screen keeps its existing "Your wish shall be granted" animation, then a button advances into the letter pages (doesn't replace the end screen).
  - Since wish input (step 6) hadn't actually been built yet despite being fully spec'd, confirmed with the user to build it this session too, so the full chain (reward → wish → letter pages) actually works end-to-end.
- **`app/components/WishInput.tsx`** (new) — step 6, built for the first time:
  - Prompt copy + textarea + submit button; POSTs JSON to the confirmed Formspree endpoint (`https://formspree.io/f/mjybrpwa`) with `Accept: application/json`.
  - On success, swaps to a full-screen dark (`bg-wine-deep`) end state: a twinkling starfield (new `.animate-twinkle` CSS keyframe — opacity/scale pulse, randomized per-star timing via `useMemo` so stars don't resync on re-render) behind "Your wish shall be granted", which rises in via an anime.js `createTimeline` (same pattern as `RewardReveal.tsx`). After `SETTLE_DELAY_MS` (4.2s) a "Continue →" button fades in (`animate-soft-reveal`) to advance to the letter pages.
  - Basic error state if the Formspree request fails (Thai retry message), doesn't block re-submission.
- **`app/components/LetterPages.tsx`** (new) — step 7, built for the first time:
  - Reuses the exact `page-flip` (StPageFlip) setup from `PhotoAlbum.tsx` (same settings, same `.photo-page` sizing class) but simplified — no beat/audio sync, no auto-flip timer, since there's no music to sync to here and it's meant to be read at her own pace.
  - `PAGES` is a hardcoded `string[][]` — currently 3 pages × 5 bracketed placeholder lines (e.g. `"[Page 2, line 1 — placeholder.]"`), clearly marked as swap-out-later content, same convention as the album's placeholder photos.
  - Each sentence renders with `.animate-soft-reveal` (reused existing keyframe, not a new one) with a per-index `animationDelay` (700ms stagger) — the "line-by-line fade-in" the user asked for. Sentences remount and replay on every flip-to, via the same `animKeys`-bump-on-flip-event pattern already used in `PhotoAlbum.tsx`.
- **`app/components/RewardReveal.tsx`** — added an `onContinue` prop and a "Continue →" button that fades in ~4.2s after the box opens (once the ticket spin/glitter timeline has had time to finish) — previously this screen had no way to move the flow forward at all.
- **`app/globals.css`** — added `@keyframes twinkle` + `.animate-twinkle` for the wish-input starfield (opacity/scale pulse loop), following the same authoring pattern as the existing `star-fall`/`sparkle` keyframes.
- **`app/page.tsx`** — added `rewardDone`/`wishDone` state and wired the full chain: PIN → quiz → album → reward reveal → wish input → letter pages. The site now has a real ending for the first time.
- Verified: `tsc --noEmit` clean. Dev server (Turbopack) compiled with no errors and served `GET / 200` with no console errors in the log.
- Not verified: haven't manually clicked through the live chain (reward → wish submit → letter pages), haven't confirmed the Formspree POST actually delivers an email in this new fetch-based implementation (previously only specified, never actually wired up), and the letter pages' sentence stagger/typewriter feel hasn't been seen live.

**Current state relative to build order:**
- Steps 1, 2, 5 done. Step 3 prototype done pending real photos. Step 4 basic playback done pending real-device check. **Step 6 (wish input) now built** — previously only spec'd. **Step 7 (letter pages) now built** as a new step, prototype with placeholder text. Step 8 (end-to-end wiring) — the flow is now fully connected start to finish for the first time; a real polish pass is still pending.

**Unfinished / partial:**
- Letter pages still needs the user's real message swapped into `LetterPages.tsx`'s `PAGES` array (currently 3×5 bracketed placeholders — adjust count/length freely when writing the real content).
- Wish input's Formspree submission has never actually been fired against the live endpoint — worth the user testing it once and checking the inbox.
- No live click-through of the reward → wish → letter chain yet; only compile/serve verified.

**Open questions for user (not blocking):**
- Does 3 pages × 5 sentences match what you had in mind, or would 2 pages / different line counts read better?
- Does the 700ms per-sentence stagger feel like a good reading pace once tried live?
- (Carried over) live feedback on reward reveal, photo album, real photos, hosting target.

**Next step:**
Click through the full flow live once (PIN → ... → reward → wish input, actually submit a test wish and check the email arrives → letter pages) to confirm the new wiring works end to end. Then: real photos for the album, real sentences for the letter pages, optional ticket image, and hosting target remain the open content/decision items.

## Entry 29 — 2026-08-13 — fixed desktop page-flip bug (photo album + letter pages)

**Done:**
- User reported the photo album wouldn't turn to another page on desktop. Investigated by temporarily installing Playwright (dev-only, uninstalled again after) to actually click through the book in a real headless Chromium browser and inspect the DOM/state, rather than guessing — confirmed via the tool that clicking did nothing usable before the fix.
- **Root cause**: StPageFlip switches from single-page "portrait" turning to a two-page side-by-side "spread" once its container is wider than `minWidth * 2` (480px, since `minWidth: 240`). Our book's wrapping `<div>` had no width cap, so with `size: "stretch"` it filled the page up to `maxWidth * 2` (920px) on any normal desktop viewport — triggering spread mode, where both of our 2 pages show at once with nothing left to flip to. Confirmed via a debug script: book bounding box was 920px wide, both page elements had `--left`/`--right` classes (spread mode) instead of one active/one hidden.
- **Fix**: wrapped the book (`<div ref={bookRef}>`) in a `<div className="w-full max-w-110">` (440px cap, comfortably under the 480px threshold) in both `PhotoAlbum.tsx` and `LetterPages.tsx` (same page-flip setup, same bug would've hit letter pages too, just never noticed yet since that step is newer). This forces single-page portrait mode on every screen size, desktop included.
- Re-ran the same Playwright debug script after the fix: book width now 440px, click correctly flips from the 2022 page to the 2023 page (confirmed via a screenshot showing the 2023 label and photo). Removed the debug script, screenshot, and the temporary Playwright dev-dependency afterward — `package.json`/`package-lock.json` confirmed clean of it.
- Verified: `tsc --noEmit` clean, dev server still serves `200` with no console errors after the revert of a temporary debug flag in `page.tsx` (used only to skip PIN/quiz while testing, reverted immediately after).

**Current state relative to build order:**
- No step-order change. Step 3 (photo album) and step 7 (letter pages) both had this same page-flip bug fixed; both remain otherwise as previously documented (album needs real photos, letter pages needs real message).

**Unfinished / partial:**
- Only tested the fix via a headless browser click at one viewport size (1280×900) and via automation, not a real mouse/trackpad drag or an actual desktop browser window — worth a quick manual click-through on your end to confirm it feels right (StPageFlip should now support both simple clicks and drag-to-flip near the page edge).
- Same carried-over items as Entry 28 (real photos, real letter sentences, Formspree live-test, ticket image, hosting target).

**Open questions for user (not blocking):**
- None new — just confirm the flip now works for you on desktop.

**Next step:**
Have the user confirm desktop flipping now works. Then: real photos for the album, real sentences for the letter pages, live Formspree test, optional ticket image, and hosting target remain the open items.

## Entry 30 — 2026-08-13 — album→reward handoff reverted from auto-timer to manual button

**Done:**
- User clarified that Entry 27's problem report ("photo page won't turn") was actually about the album→reward-reveal handoff, not intra-album page flipping (which Entry 29 already fixed) — the automatic timer-driven `onContinue()` call added in Entry 27 wasn't advancing into the reward reveal as expected. Asked to revert that part and use a manual button instead.
- `PhotoAlbum.tsx`: the auto-flip timer (`AUTO_FLIP_DELAY_MS`, 4.5s after a page's photos finish flying in) now only fires `pageFlip.flipNext()` for pages before the last one — it no longer calls `onContinue()` automatically on the last page. Added a `isLastPage` flag and re-added a "Continue to your surprise →" button (same style/behavior as the original Entry-17-era placeholder link), now shown once the last page's photos have finished flying in, calling `onContinue()` on tap.
- Auto-flip **between** album pages is unchanged and still timer-driven; only the final album→reward transition changed from automatic to manual.
- Verified: `tsc --noEmit` clean, dev server (already running from the prior entry) still serves `200` with no console errors after the edit.

**Current state relative to build order:**
- No step-order change. Step 3 (photo album) — same as Entry 29, plus this handoff-behavior revert. Step 5 (reward reveal) unaffected.

**Unfinished / partial:**
- Not manually clicked through live to confirm the button appears/works as expected — same standing caveat as before, verified via compile + serve only (plus the Entry 29 Playwright check, which didn't cover this specific button since it predates this change).
- Same carried-over items as Entry 29 (real photos, real letter sentences, Formspree live-test, ticket image, hosting target).

**Open questions for user (not blocking):**
- None new — confirm the "Continue to your surprise →" button now shows up and works on the last page.

**Next step:**
User to confirm the album→reward handoff now works via the button. Then: real photos for the album, real sentences for the letter pages, live Formspree test, optional ticket image, and hosting target remain open.

## Entry 31 — 2026-08-13 — fixed the "Continue to your surprise" button never appearing

**Done:**
- User reported that even after Entry 30's manual button was added, the album→reward-reveal handoff still didn't work. Investigated with the same real-browser approach as Entry 29 (temporarily reinstalled Playwright, skipped PIN/quiz via a temp debug flag, clicked through) instead of guessing again.
- **Root cause**: the button from Entry 30 was gated behind `currentPageComplete` (all of the last page's photos having flown in, which is driven by beat detection). Beat detection depends on the browser actually letting the audio autoplay and produce beats — if autoplay is blocked (very common without a prior user gesture) or the song's last section is quiet, `currentPageComplete` can just never become `true`, so the button never rendered at all, with no visible error or feedback.
- **Fix**: removed the `currentPageComplete` gate — the button now shows as soon as she's on the last page (`isLastPage`), regardless of whether the beat-synced photo entrance has finished. It no longer depends on audio/beat state at all.
- Verified live via Playwright: flipped to the last page, confirmed the button appeared immediately (not just after a wait), clicked it, and confirmed the reward reveal screen ("Your Surprise" heading) actually rendered next. Removed the debug script/screenshot and the temporary Playwright dev-dependency afterward, reverted the temp PIN/quiz skip in `page.tsx`.
- Verified: `tsc --noEmit` clean.

**Current state relative to build order:**
- No step-order change. Step 3 (photo album) — album→reward handoff now confirmed working live for the first time (previous entries were compile/serve-verified only, which is exactly how this bug slipped through twice).

**Unfinished / partial:**
- Same carried-over items as Entry 30 (real photos, real letter sentences, Formspree live-test, ticket image, hosting target).

**Open questions for user (not blocking):**
- None — this was confirmed working live before closing out, not just assumed.

**Next step:**
Real photos for the album, real sentences for the letter pages, live Formspree test, optional ticket image, and hosting target remain the open items.

## Entry 32 — 2026-08-13 — added "song's not over" interstitial before letter pages

**Done:**
- User asked for a new screen before the letter pages: text "เพลงยังไม่จบเลย เพราะงั้นอ่านต่ออีกนิดนึงนะ" with falling stars in the background. Clarified via questions: it's a new standalone step (wish input → this → letter pages, not replacing wish-input's own end screen), auto-advances after a few seconds (no tap), and uses the ivory background (matching the main site look, not wish-input's dark end screen).
- **`app/components/SongContinues.tsx`** (new) — reuses the exact falling-star pattern already established in `RewardReveal.tsx` (`.animate-star-fall`, randomized per-star size/duration/delay/drift via `useMemo` so they don't resync), just on the default ivory background instead of behind an opened box. Auto-calls `onContinue` after `AUTO_ADVANCE_MS` (4s) via a `setTimeout` in a `useEffect` — no button, per the user's answer.
- **`app/page.tsx`** — added `songMessageDone` state and inserted `SongContinues` into the chain between `WishInput` and `LetterPages`.
- Verified: `tsc --noEmit` clean, dev server (already running) still serves `200` with no console errors.

**Current state relative to build order:**
- New step added to the flow (renumbered step 7 in `plan.md`; letter pages is now step 8, end-to-end wiring is step 9). Flow is now: PIN → quiz → album → reward → wish input → song's-not-over interstitial → letter pages, fully wired.

**Unfinished / partial:**
- Not manually clicked through live (compile/serve-verified only this time) — given the last two entries were bugs that only compile/serve verification missed, worth a real click-through at some point, though this component is simple enough (no page-flip, no audio/beat dependency) that the same class of bug is unlikely here.
- Same carried-over items as Entry 31 (real photos, real letter sentences, Formspree live-test, ticket image, hosting target).

**Open questions for user (not blocking):**
- Does 4 seconds feel like the right amount of time for this screen, or should it linger longer/shorter?
- (Carried over) live feedback on the rest of the flow.

**Next step:**
Real photos for the album, real sentences for the letter pages, live Formspree test, optional ticket image, and hosting target remain the open items.

## Entry 33 — 2026-08-13 — wish-input end screen now auto-advances instead of requiring a tap

**Done:**
- User asked to change the "Your wish shall be granted" screen from manual-tap to automatic advance into the next step.
- `WishInput.tsx`: removed the `settled` state and the "Continue →" button entirely. The `setTimeout` that previously flipped `settled` to `true` (revealing the button) now calls `onContinue` directly after `SETTLE_DELAY_MS` (4.2s) — same timing as before, just no tap required.
- This makes the wish-input step consistent with the "song's not over" interstitial right after it (Entry 32), which also auto-advances — the reward reveal and photo album remain manual-tap by design (per Entries 30/31, since tying advancement to animation/beat completion proved unreliable there).
- Verified: `tsc --noEmit` clean, dev server still serves `200` with no console errors.

**Current state relative to build order:**
- No step-order change. Step 6 (wish input) behavior updated only.

**Unfinished / partial:**
- Not manually clicked through live — compile/serve-verified only. Low risk here since the change is a straightforward `setTimeout` swap with no page-flip/beat-detection involved (the class of bug that bit the last two entries).
- Same carried-over items as Entry 32 (real photos, real letter sentences, Formspree live-test, ticket image, hosting target).

**Open questions for user (not blocking):**
- None new.

**Next step:**
Real photos for the album, real sentences for the letter pages, live Formspree test, optional ticket image, and hosting target remain the open items.

## Entry 34 — 2026-08-13 — added warp-speed transition into the "song's not over" screen

**Done:**
- User asked for a "warp effect" during the wish-input → song's-not-over transition. Clarified via question: star-streak hyperspace zoom (stars stretch into radiating lines and rocket outward), as opposed to a simple zoom-blur flash or a swirl/vortex.
- `WishInput.tsx`:
  - Added `triggerWarp()` — an anime.js timeline that, on each twinkling star, rotates it to point away from screen-center (computed via `atan2` from the star's stored `left`/`top` position), elongates it (`scaleY` 10-18x, `scaleX` 0.35), and translates it further outward along that same direction, fading to 0 opacity — the classic hyperspace-streak look. Runs alongside the "Your wish shall be granted" text zooming/fading out and a radial white/gold flash (new `flashRef` div, same gradient style as `RewardReveal`'s light burst) peaking at the same time.
  - Before the warp animation starts, each star's looping CSS `.animate-twinkle` is paused (`animationPlayState = "paused"`) — otherwise the CSS keyframe kept overwriting `transform` every frame and fought with anime.js driving the same property, causing jitter.
  - `onContinue` now fires from the warp timeline's `onComplete` (on the flash animation) instead of directly from the settle `setTimeout` — the timeout now calls `triggerWarp` instead of `onContinue` directly, so the full sequence is: submit → starfield/text reveal → (4.2s) → warp streak + flash (~650ms) → next screen.
  - TypeScript needed `as any` casts on the three per-star function-value props (`rotate`, `translateX`, `translateY`) — animejs v4's types couldn't cleanly infer a 2-arg per-target callback against the property's union type; runtime behavior is unaffected, this is purely a type-narrowing workaround (documented inline).
- Verified live (not just compile/serve) via a temporary Playwright script, per the lesson from Entries 29/31: skipped to wish-input, intercepted the real Formspree network call so no actual test email got sent, submitted a wish, and confirmed both "Your wish shall be granted" appeared and — after waiting past the settle + warp durations — the song's-not-over screen rendered next, with no console/page errors. Screenshot confirmed the final screen visually. Removed the debug script/screenshot and the temporary Playwright dependency afterward, reverted the temp flow-skip flags in `page.tsx`.
- Verified: `tsc --noEmit` clean.

**Current state relative to build order:**
- No step-order change. Step 6 (wish input) now has the warp transition; confirmed working live end-to-end into step 7.

**Unfinished / partial:**
- Only tested at one viewport size (1280×900) via automation — worth a look on an actual phone/browser to judge the timing/feel subjectively (that's inherently not something automated testing can judge).
- Same carried-over items as Entry 33 (real photos, real letter sentences, Formspree live-test on the real endpoint, ticket image, hosting target).

**Open questions for user (not blocking):**
- Does the warp duration (~650ms) and intensity feel right, or should it be longer/more dramatic?

**Next step:**
Real photos for the album, real sentences for the letter pages, live Formspree test (on the real endpoint, not the intercepted test one), optional ticket image, and hosting target remain the open items.

## Entry 35 — 2026-08-18 — recreated missing source_file/images and source_file/ticket folders

**Done:**
- User asked what's next; per plan.md's build order the last open content items are real photos, real letter text, and hosting target. User chose to tackle real photos first.
- Found `source_file/images/` and `source_file/ticket/` missing from disk entirely (only `source_file/Audio/` remained) — contradicts plan.md/handoff.md, which describe them as existing (even if empty) since Entries 7 and 26. Flagged to user rather than guessing why, per this project's ground rules; user asked to just recreate them.
- Recreated `source_file/images/2022/`, `source_file/images/2023/` (year subfolders per the plan's naming convention), and `source_file/ticket/`. All currently empty.
- **No app code changed this entry** — folder recovery only.

**Current state relative to build order:**
- Unchanged from Entry 34 — steps 1-7 built, step 8 (letter pages) has placeholder text, step 9 (polish) pending. Folders for pending content now exist again, still empty.

**Unfinished / partial:**
- Photos not yet dropped in — waiting on user to populate `source_file/images/2022/`, `2023/` (and any other year folders they add) via File Explorer.
- Real letter-page sentences, hosting target, ticket image, and live Formspree test on the real endpoint remain open (unchanged from Entry 34).

**Open questions for user (not blocking):**
- Worth a check of the user's OS/backup history for why these two folders (but not Audio/) disappeared, if it happens again — not investigated this session.

**Next step:**
Once photos are dropped into `source_file/images/<year>/`, swap them into `PhotoAlbum.tsx`/`YearPage.tsx` in place of the picsum.photos placeholders. Real letter text, hosting target, ticket image, and a real-endpoint Formspree test remain open after that.

## Entry 36 — 2026-08-18 — real photos (2020–2026) wired into the photo album

**Done:**
- User dropped real photos into `source_file/images/<year>/` for years 2020 through 2026 (1–6 photos each, filenames are camera-assigned numbers like `812419_0.jpg` rather than the `01.jpg`-style convention floated earlier — didn't rename them, just sorted by filename as-is per plan.md's "ordered by filename within each year" rule, which still holds regardless of naming scheme).
- Copied all seven year folders from `source_file/images/` into `public/images/<year>/` (Next.js only serves files under `public/`, same reasoning as the Entry 18 audio-file move).
- `PhotoAlbum.tsx`: replaced the `YEARS` placeholder array (picsum.photos, 2 years) with the real 7-year, real-photo array, paths pointing at `/images/<year>/<filename>.jpg`. Removed the now-inaccurate "placeholder photos — real ones swap in later" label text from the rendered page. No changes to `YearPage.tsx`, the collage/scatter-slot logic, chunking/overflow logic, or the beat-sync/auto-flip logic — all of that was already written to be content-agnostic per Entry 17's original design intent.
- Verified: `tsc --noEmit` shows one pre-existing, unrelated error in `app/layout.tsx` (`Cannot find name 'LayoutProps'`) not touched by this change — confirmed via `git status`/`git diff --stat` that only `PhotoAlbum.tsx` was modified. Started the dev server directly and confirmed `GET /` returns 200 with no console errors, and spot-checked two of the real image URLs (`/images/2020/812413.jpg`, `/images/2026/812453_0.jpg`) both return 200. Stopped the dev server after verification.
- Updated `plan.md`: removed "photos for the album" from the pending-content checklist (only ticket image and letter-page text remain), marked build-order step 3 as having real photos wired in.
- **Not done this entry**: did not investigate the pre-existing `LayoutProps` TS error (out of scope, unrelated to this task — flagging so it isn't mistaken for something this change introduced). Did not click through the live album in a browser to eyeball the real-photo collage/pacing.

**Current state relative to build order:**
- Step 3 (photo album) now has real photos across all 7 years (2020–2026) instead of picsum placeholders. Steps 1, 2, 5 unchanged (done). Step 4 unchanged (basic playback done). Step 6 unchanged (behavior settled per Entries 33–34). Step 7 (song's-not-over) unchanged. Step 8 (letter pages) still has placeholder text.
- Only two content items remain on the whole plan: real letter-page sentences and the optional ticket image. Hosting target is the one remaining open technical decision.

**Unfinished / partial:**
- Not visually confirmed live in a browser (only server/HTTP-level verification this entry) — worth a look to see how the real photos read in the collage layout (aspect ratios/crops weren't curated, `object-cover` on a fixed `h-42 w-36` box will crop unevenly depending on each photo's original dimensions).
- Pre-existing `LayoutProps` TS error in `app/layout.tsx` noticed but not investigated — unrelated to this session's change, flagging in case it matters later.
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 35).

**Open questions for user (not blocking):**
- How do the real photos actually look in the collage once you try it live — any crops/orientations that look wrong given the fixed portrait photo-box size?

**Next step:**
Try the album live to sanity-check the real-photo collage. Then: real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.

## Entry 37 — 2026-08-18 — fixed photo entrances getting stuck at 1 photo per page

**Done:**
- User reported some years with 6 photos only ever showed 1. Investigated live instead of guessing (same approach as Entries 29/31/34): temporarily reinstalled Playwright, added a temp `?skip=album` query-param debug flag to `page.tsx` to jump straight past PIN/quiz, flipped through the album, and inspected the DOM directly.
- **Confirmed root cause**: every page's photo entrance is gated entirely on `beatCount`, which only increments from real-time Web Audio energy-threshold beat detection (Entry 18). If the browser doesn't produce usable beats — autoplay blocked, headless/no audio device, or detection just not firing — `beatCount` never advances, so `visibleCount` stays frozen at 1 forever. `plan.md`'s documented "known limitation" only anticipated losing beat-*sync*, not photos getting stuck unrevealed entirely; this was a real regression against that intent, not a documented tradeoff.
- **Fix** in `PhotoAlbum.tsx`: added a `FALLBACK_BEAT_MS` (900ms) fallback inside the existing beat-detection `tick()` loop — if no real beat has landed within that window, a synthetic beat fires anyway (`isFallbackBeat`), advancing `beatCount` on a steady pulse. Real beats still take priority and reset the timer whenever they're actually detected; the fallback only kicks in when they're not. This is a small addition to the existing loop, not a new system — no changes to `YearPage.tsx` or the chunking/collage logic.
- Verified live via the same Playwright script: before the fix, every page capped at 1 visible photo no matter how long it sat active; after the fix, pages given enough active time filled in fully (2022 confirmed 4/4, 2023 5/5, 2024 6/6). Removed the debug script/screenshot and the temporary Playwright dev-dependency afterward; reverted the temp `?skip=album` flag in `page.tsx` (confirmed via `git status` it's no longer in the diff).
- Verified: dev server restarted clean, `GET /` returns 200 with no console errors in the log.

**Current state relative to build order:**
- Step 3 (photo album) — real photos (Entry 36) now actually reveal correctly on every page instead of getting stuck at 1. No other step affected.

**Unfinished / partial:**
- Fallback timing (900ms) is a first-pass guess, not tuned against how the real song's beats land — worth a live listen-through to see if the fallback ever visibly kicks in against real audio (vs. just filling in smoothly on real beats) and whether 900ms feels right if it does.
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 36).

**Open questions for user (not blocking):**
- Please confirm the album now shows all your photos per year when you try it live.

**Next step:**
User to confirm the album fully reveals all photos per year now. Then: real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.

## Entry 38 — 2026-08-18 — fixed collage overlap; replaced StPageFlip with a warp-transition pager (album + letter pages)

**Done:**
- User reported two things: with 6 photos on a page, the last one collides with others; and the page-turn animation itself feels buggy. Asked to remove page-turning in favor of a warp animation instead.
- **Collage overlap** — root-caused directly from the `SLOTS` data in `YearPage.tsx` (no live testing needed to see it): slot indices 2 and 5 sat at nearly identical positions (`top: 6%/0%, left: 68%` both), so the 3rd and 6th photo on any page landed almost fully stacked. First attempted a 3-row x 3-column redesign, but live bounding-box measurement via a temporary Playwright script showed the fixed 168px-tall photo box doesn't leave enough vertical room for 3 rows at typical page heights (row-vs-row overlap persisted). Landed on 2 rows x 4 distinct columns instead (matches the original design's row spacing, which was already proven to fit) — re-measured live and confirmed zero row-vs-row overlap, with only the intentional "slight" same-row adjacent-column overlap remaining (the scrapbook look the plan calls for). Confirmed visually via screenshot too.
- **Replaced StPageFlip entirely**, per the user's explicit ask and confirmed via clarifying questions: scope is both the photo album and letter pages (not just the album), the warp style reuses the existing hyperspace star-streak + flash effect already built for `WishInput.tsx`'s screen transition (not a new visual design), and navigation is dedicated Back/Next buttons (not tap-anywhere) so she can also go back to a previous year/page, not just forward.
  - Extracted the warp effect out of `WishInput.tsx` into a new shared hook, **`app/components/useWarpTransition.tsx`** — renders the twinkling-star background + flash overlay, and exposes `warp(onSwap)` to play the star-streak/flash timeline and swap page content via `onSwap` right as the flash peaks, then fades the new content back in and resets the stars for the next warp. `WishInput.tsx` itself was left untouched (its own inline warp logic still works and wasn't asked to change) — the extraction only affects the album/letter pages, which are new consumers of the shared hook.
  - **`PhotoAlbum.tsx`** rewritten: removed `page-flip` import, `PageFlip` instance, `bookRef`, and the `.photo-page` book markup. Now renders only the *current* page (a fixed-aspect-ratio card, `aspect-300/380` capped at 440px wide — same proportions the book used) inside the warp hook's `contentRef`, with Back/Next buttons below. The existing beat-sync/fallback-pulse photo-entrance logic (Entry 37) and the hybrid auto-advance timer both carry over unchanged — auto-advance now calls the new `goTo()` (which triggers a warp) instead of `pageFlip.flipNext()`. The last-page "Continue to your surprise →" button behavior (Entry 31) is unchanged.
  - **`LetterPages.tsx`** rewritten the same way: removed `page-flip`, renders the current page's sentences (unchanged stagger/fade-in behavior) inside a matching fixed-aspect card, with Back/Next buttons — no auto-advance here, same as before.
  - **Removed the `page-flip` npm dependency** entirely (`npm uninstall page-flip`), deleted its now-unused TypeScript shim `app/types/page-flip.d.ts`, and removed the inlined StPageFlip core CSS block (`.stf__parent`/`.stf__block`/`.stf__item`/etc. and `.photo-page`) from `globals.css` — confirmed via `grep` that nothing else in the codebase referenced any of it before deleting.
- Verified: `tsc --noEmit` clean (also incidentally confirmed a stray `LayoutProps` error seen in Entry 36 was a stale Next.js type-generation artifact, not a real issue — gone after a dev server run, unrelated to this change). Live-verified via a temporarily reinstalled Playwright (same pattern as Entries 29/31/34/37, removed afterward along with the temporary `page.tsx` debug flag): confirmed Back/Next actually swap the visible year/page content in both components, screenshotted both mid-transition to confirm the star/flash/card visuals render correctly against the ivory background, and re-ran the overlap bounding-box check to confirm the new collage slots. `git status` confirms the temp debug flag left no trace in `page.tsx`.
- **plan.md** updated: Stack section no longer lists `page-flip`; step 3 and step 8's flow descriptions now describe the warp-transition pager instead of StPageFlip; build-order step 3 line updated with both fixes.

**Current state relative to build order:**
- Step 3 (photo album) and step 8 (letter pages) both now use the warp-transition pager instead of StPageFlip — a real architecture change, not a bug patch, made with the user's explicit sign-off on scope/style/navigation via clarifying questions first. Collage overlap bug is fixed and verified live. No other steps affected.

**Unfinished / partial:**
- Not tested at a real narrow mobile viewport (only 1280×900 via automation) — the collage's same-row column overlap is somewhat viewport-width-dependent (fixed-pixel photo boxes vs. percentage-based container), worth a look on an actual phone.
- Warp transition timing (650ms + 400ms fade-in) is carried over as-is from `WishInput.tsx`'s tuned values, not re-tuned for this faster, more-frequent navigation context (she'll trigger it far more often than the one-time wish-input transition) — may feel like it needs to be snappier once tried live at that frequency.
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 37).

**Open questions for user (not blocking):**
- Does the warp transition feel right at album/letter-page navigation frequency, or does it need to be faster given how often she'll trigger it compared to the one-time wish-input use?
- Please confirm the collage no longer overlaps and the warp/Back/Next navigation feels good once tried live on your end.

**Next step:**
User to try the album and letter pages live end-to-end. Then: real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.

## Entry 39 — 2026-08-18 — collage rebuilt as a real CSS grid; bigger photos, 3-per-row for 6-photo years

**Done:**
- User asked for bigger photos, specifically so 6-photo years show 3 per row.
- Replaced `YearPage.tsx`'s hand-placed absolute `SLOTS` array (a source of the overlap bugs fixed in Entries 38/39) with an actual CSS grid (`grid-cols-3`) — cells can't overlap by construction, and each photo now sizes off its grid cell (`aspect-3/4 w-full`) instead of a fixed 168px pixel box, so it scales with however much room the page card has rather than a hardcoded size. Rotation per photo (reused the existing tilt values) keeps the scrapbook feel even though positions are now a tidy grid.
- Widened the album's page card (`PhotoAlbum.tsx`) from a 440px cap to a 560px cap so the wider grid has real room — same aspect ratio as before (300/380), just scaled up.
- Note: this moves away from `plan.md`'s original "scattered/collage... not a uniform grid" description (Entry 15) — the user's explicit ask this session (3-per-row) is inherently more grid-like than that spec, so `plan.md`'s step 3 description was updated to match what was actually asked for and built, rather than left contradicting the code.
- Verified live via a temporarily reinstalled Playwright (removed after, same pattern as recent entries; temp `page.tsx` debug flag reverted, confirmed absent via `git status`): navigated to the 2024 page (6 real photos) and measured actual rendered photo boxes — grew from the old fixed 144x168px to ~180-187px wide x ~230px tall, arranged exactly 3-per-row x 2 rows as asked. Two adjacent pairs showed a ~2px bounding-box overlap, traced to the rotation transform's corner poking slightly past the cell edge — visually negligible (vs. the 30-150px near-total overlaps fixed in Entry 38) and confirmed as fine via a screenshot. `tsc --noEmit` clean, dev server serves 200 with no console errors.
- Updated `plan.md`'s step 3 description (grid-based layout, new 560px width cap) to match.

**Current state relative to build order:**
- Step 3 (photo album) — collage layout rebuilt as a grid; bigger, cleanly-arranged photos confirmed live. No other steps affected (letter pages don't use a photo grid, untouched this entry).

**Unfinished / partial:**
- Only checked at one desktop viewport (1280x900) — worth a look on an actual phone to see how the grid reflows at narrow widths (photos should shrink to fit 3 columns, per the relative sizing, but not visually confirmed at small widths).
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 38).

**Open questions for user (not blocking):**
- Please confirm the bigger 3-per-row grid looks/feels right once tried live, especially on your phone.

**Next step:**
User to confirm the new grid layout live, ideally on mobile. Then: real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.

## Entry 40 — 2026-08-18 — slowed warp transition to 0.75x speed; song no longer loops

**Done:**
- User asked for two things: the transition effect a little slower (~0.75x speed), and the song to start from the same point but play through to its natural end instead of looping.
- **Warp transition speed**: `useWarpTransition.tsx`'s `WARP_DURATION_MS` (the shared hook used by the photo album and letter pages) went from 650ms to 870ms (650 / 0.75 ≈ 867, rounded), and its post-warp fade-back-in duration scaled proportionally (400ms → 530ms). Also updated `WishInput.tsx`'s own separate `WARP_DURATION_MS` constant (its warp-into-song-continues transition uses the same visual effect but predates the shared hook, so it's a separate copy) to the same 870ms, for consistency — the user's ask didn't specify scope, but since it's visually the same effect, leaving one faster than the other would read as a mistake rather than a deliberate choice. Flagging this scope call in case only the album/letters transition was meant.
- **Song looping**: removed the `loop` attribute from the `<audio>` element in `PhotoAlbum.tsx`. It still starts playing from the beginning the same way it always has (autoplay attempt on album mount, unchanged) — the only change is that once it reaches the end, it now stops naturally instead of restarting from 0. No changes needed to the beat-detection logic: the existing fallback-pulse (Entry 37) already covers the case where the song has ended and produces silence, so photo entrances keep advancing even after the song stops.
- Verified: `tsc --noEmit` clean, dev server serves 200 with no console errors.

**Current state relative to build order:**
- No step-order change. Step 3 (photo album, music) and step 6 (wish input) both have the updated warp timing; step 4 (music) no longer loops.

**Unfinished / partial:**
- Not manually clicked through live this entry (compile/serve-verified only) — low risk, this is a straightforward constant-value change with no logic restructuring, but worth a real listen/watch to confirm 0.75x actually feels like "a little slower" as intended rather than over/under-corrected.
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 39).

**Open questions for user (not blocking):**
- Was the warp-speed slowdown meant for the album/letter-page transitions only, or also the wish-input → song-continues warp? Applied to both for visual consistency — say if you wanted just the former.
- Does 0.75x feel like the right amount of "a little slower," or does it need further adjustment?

**Next step:**
User to confirm the new transition pace and non-looping song live. Then: real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.

## Entry 41 — 2026-08-18 — song now persists across the whole site, not just the album

**Done:**
- User clarified Entry 40's "keeps playing until the end" meant the end of the whole website, not just not-looping within the album — the song was still cutting off the moment she left the album, since the `<audio>` element and its beat-detection lived inside `PhotoAlbum.tsx` and unmounted along with it.
- Built **`app/components/MusicProvider.tsx`** — a new context provider that owns the `<audio>` element, its `AudioContext`/`AnalyserNode` setup, and the energy-threshold beat detection (including the fallback pulse from Entry 37), moved verbatim out of `PhotoAlbum.tsx`. Exposes the live beat count via a `useMusicBeat()` hook for any step that wants it (currently only the album, for its photo entrances).
- **`PhotoAlbum.tsx`**: removed its own `<audio>` element, `AudioContext`/analyser setup, and beat-detection effect entirely — now just calls `useMusicBeat()` to read the shared beat count. Its own beat-driven entrance/auto-advance logic is otherwise unchanged.
- **`page.tsx`**: restructured from a chain of early-return `if` statements (each of which fully unmounted the previous step, including whatever it rendered) into a single `MusicProvider` wrapping every step from the album onward, with the actual step chosen by conditional rendering inside it. Since `MusicProvider` itself never unmounts once she reaches the album, the one `<audio>` element it owns survives every subsequent step change — reward reveal, wish input, song's-not-over, letter pages — playing continuously start to finish (or until she reaches the last screen, whichever comes first, since it no longer loops per Entry 40).
- Verified live (not just compile) via a temporarily reinstalled Playwright, launched with `--autoplay-policy=no-user-gesture-required` so headless Chromium would actually play audio: tagged the `<audio>` DOM node on load, then clicked through the album's last page into the reward reveal and confirmed (a) the tagged node identity never changed — same element the whole way, not remounted — and (b) `currentTime` climbed continuously across the transition (1.4s → 3.4s → 12.8s → 15.9s) with `paused: false` throughout, i.e. actual uninterrupted playback, not just a plausible-looking DOM structure. Removed the debug script and the temporary Playwright dependency afterward; reverted the temp `page.tsx` debug flag used to skip PIN/quiz for the test (confirmed via `git diff app/page.tsx` that only the real `MusicProvider` restructuring remains).
- Verified: `tsc --noEmit` clean, dev server serves 200 with no console errors.
- Updated `plan.md`'s step 4 description to document the new persistence behavior and where it lives.

**Current state relative to build order:**
- No step-order change. Step 4 (music) now actually matches "plays in the background" for the whole rest of the site, not just the album. Step 3 (photo album) unaffected behaviorally — same beat-sync entrance logic, just sourced from context instead of owning the audio itself.

**Unfinished / partial:**
- Only verified the album→reward-reveal handoff live; didn't click all the way through wish input → song's-not-over → letter pages in this session, though the mechanism (MusicProvider never unmounting) applies identically to all of them.
- Real letter-page sentences, hosting target, ticket image, and a live Formspree test on the real endpoint remain open (unchanged from Entry 40).

**Open questions for user (not blocking):**
- None new — this was confirmed working live (actual playback continuity, not just structural) before closing out.

**Next step:**
Real letter-page sentences, hosting target, optional ticket image, and a live Formspree test remain the open items.
